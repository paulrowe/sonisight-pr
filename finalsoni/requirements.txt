# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Image / numerics
numpy>=1.24,<3.0
opencv-python>=4.8
Pillow>=10.0

# ML pipeline
scikit-image>=0.22
scikit-learn>=1.4
joblib>=1.3

# Optional: U-Net backend. Install separately via:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu  (CPU)
#   pip install torch                                                     (default)
# Leaving torch un-pinned here so a CPU/CUDA wheel can be chosen explicitly.
# torch>=2.1
